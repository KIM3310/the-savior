window.THE_SAVIOR_RUNTIME = {
  // Optional: override API base URL.
  // Native builds fall back to the production Pages origin.
  // Web builds first try the same-origin Pages Functions surface.
  // Set this explicitly only when the API should point at a different backend origin.
  // Example: "https://the-savior-9z8.pages.dev"
  apiBaseUrl: ""
};
