window.THE_SAVIOR_RUNTIME = {
  // Optional: override API base URL.
  // Native builds fall back to the production Pages origin.
  // Web production should set this explicitly; otherwise the app stays in review-only mode.
  // Example: "https://the-savior-9z8.pages.dev"
  apiBaseUrl: ""
};
